{
  "buildCommand": "pnpm --filter @ceicavs/shared build && pnpm --filter @ceicavs/web build && cp -r apps/web/dist dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
