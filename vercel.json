{
  "buildCommand": "pnpm --filter @ceicavs/shared build && pnpm --filter @ceicavs/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
